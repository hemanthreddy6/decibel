function main <-() {
    track1: audio <- load "song1.wav";
    filteredTrack1: audio <- HIGHPASS(;);  
    play filteredTrack1;

    track2: audio <- load "song1.wav";
    filteredTrack2: audio <- LOWPASS(;);  
    play filteredTrack2;
}