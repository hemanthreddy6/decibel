function checkFrequency <- (freq: float): bool {
    if freq > 440 
        return true;
    otherwise 
        return false;
}

function main <-() {
    result: bool <- checkFrequency(450);
    if (result) {
        play audio(SIN(), 450hz, 5s);
    }
}
